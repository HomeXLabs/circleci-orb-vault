version: 2.1
jobs:
  build:
    description: |
      Tests the individual components
    docker:
      - image: circleci/node
    steps:
      - vault/install

workflow:
  build:
    jobs:
      build
