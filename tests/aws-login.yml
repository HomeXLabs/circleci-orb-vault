version: 2.1
jobs:
  build:
    description: |
      Tests the individual components
    docker:
      - image: circleci/node
    steps:
      - run:
          name: Get Env
          command: |
            source test/bash.env
      - vault/aws-login

workflow:
  build:
    jobs:
      build
